pre-commit:
  parallel: true
  commands:
    ruff_lint:
      glob: "*.py"
      run: ruff check {staged_files} --fix
    ruff_format:
      glob: "*.py"
      run: ruff format {staged_files}
    unit_test:
      glob: "*.py"
      run: uv run pytest

